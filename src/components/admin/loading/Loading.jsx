import React from 'react';
import './loading.css';

const Loading = () => {
    return (
        <div className='loadingScreen--admin'>
            <div className="spinner spinner--admin"></div>
        </div>
    );
};

export default Loading;